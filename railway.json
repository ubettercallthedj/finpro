{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app/backend && composer install --no-dev --optimize-autoloader && cd /app/frontend && npm install && npm run build && cp -r dist/* /app/backend/public/"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && php artisan route:list && php artisan migrate --force && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
